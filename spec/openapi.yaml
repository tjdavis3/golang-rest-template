info:
  license:
    name: MIT
  title: API
  version: 1.0.0
openapi: 3.0.0

paths:
  
components:
  schemas:
  responses:
    ErrResponse:
      properties:
        msg:
          type: string
        request_id:
          type: string
      type: object
